#version 400 core

in vec4 colour;
in vec3 unitLight;
in vec3 unitCamera;
in vec3 vertexNormal;

out vec4 out_colour;

void main(void){
	
	float lightCameraDot = dot(unitLight, vertexNormal);
	float brightness = max(0.2,lightCameraDot);
	
	
	out_colour = colour*(brightness);

}