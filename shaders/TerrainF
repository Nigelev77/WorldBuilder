#version 400 core

in vec4 colour;
in vec3 unitLight;
in vec3 unitCamera;


out vec4 out_colour;

void main(void){
	
	float lightCameraDot = dot(unitLight, unitCamera);
	float brightness = max(0,lightCameraDot);
	
	
	out_colour = colour*brightness;

}